function make_shirt(size: string, message: string) { // The function should print // a sentence summarizing the size of the shirt and the message printed on it 
  console.log("You ordered a " + size + " shirt with the message: " + message); }

  // Call the function 
make_shirt("large", "Hello World!");
